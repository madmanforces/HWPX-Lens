# v0.1.1 Change Set JSON Export — Manual Validation Required

자동 테스트로 대체할 수 없는 실제 데스크톱 사용 검증 목록이다. 수동 검증용 설치파일은 생성했지만 물리 PC에서 아래 절차를 수행하지 않았으므로 모든 항목을 `PENDING`으로 둔다. 자동 검증 결과는 참고 증거일 뿐 수동 PASS로 바꾸지 않는다.

## Build

- Source version: `0.1.1`
- Change Set schemaVersion: `1.0.0`
- Default renderer: SVG
- Installer: 비공개 로컬 검증본 생성 완료 — 파일·프로필·checksum은 Git-ignored 검증 폴더에서만 관리
- GitHub push/release/publication: 수행하지 않음
- 권장 대상: 로컬 검증판과 사용자가 직접 확인 가능한 previous/latest HWPX pair

## Checklist

### 1. 완전 offline 실행

- Expected: 네트워크를 끈 Windows PC에서 앱 실행, 비교, 내보내기와 저장이 모두 동작한다.
- Actual: 물리 PC 검증 미실행.
- Result: **PENDING**
- Evidence: 자동 `audit:offline`은 local asset과 bundled rhwp WASM만 확인하여 PASS.

### 2. 문서 선택 순서

- Expected: 이전 HWPX를 `ORIGINAL`, 최신 HWPX를 `MODIFIED`에 연다.
- Actual: 실제 검증 pair 선택 미실행.
- Result: **PENDING**
- Evidence: synthetic E2E는 이 순서로 파일을 열어 PASS.

### 3. 역할 표시

- Expected: UI에서 Original과 Modified가 혼동되지 않고, JSON에는 각각 `previous`와 `latest`로 기록된다.
- Actual: 실제 화면 육안 검증 미실행.
- Result: **PENDING**
- Evidence: schema/unit/E2E role assertion PASS.

### 4. 변경과 Review Ink

- Expected: text, outline, table, image 변경과 양쪽 Review Ink 위치가 실제 문서와 맞는다.
- Actual: 대표 실제 변경 pair 육안 대조 미실행.
- Result: **PENDING**
- Evidence: 기존 Review Ink E2E와 전체 회귀 테스트 PASS.

### 5. 경고와 저장 대화상자

- Expected: `Change Set JSON 내보내기`를 누르면 파일명·변경 텍스트·목차 경로·위치·지문의 민감성 및 이미지 binary 미포함 경고가 나오고, 확인 후 `.json` 저장 대화상자가 열린다.
- Actual: native Windows dialog 수동 확인 미실행.
- Result: **PENDING**
- Evidence: 브라우저 E2E에서 경고 전문과 저장 흐름 PASS; Tauri plugin Rust compile PASS.

### 6. 취소 안전성

- Expected: 경고 또는 저장 대화상자를 취소해도 파일이 생기지 않고 비교·선택 상태가 유지된다.
- Actual: native dialog 수동 확인 미실행.
- Result: **PENDING**
- Evidence: E2E 경고 취소와 mocked Tauri save-dialog 취소 테스트 PASS.

### 7. 파일 인코딩과 parse

- Expected: 저장 파일은 `.json`, UTF-8 no BOM, LF, 마지막 LF 한 개이며 다시 parse할 수 있다.
- Actual: native 저장 파일 독립 검사 미실행.
- Result: **PENDING**
- Evidence: serializer unit test와 download E2E PASS.

### 8. 원시 파일 facts

- Expected: `fileName`은 basename이고 `byteLength`와 `sha256`은 사용자가 선택한 raw HWPX와 같다.
- Actual: 사용자 파일에 대한 독립 hash 비교 미실행.
- Result: **PENDING**
- Evidence: export-time raw file 재읽기, unit integrity 검증과 E2E Node SHA-256 대조 PASS.

### 9. comparisonId 독립 재계산

- Expected: 문서 SHA 두 개와 schemaVersion의 canonical preimage로 계산한 ID가 JSON과 같다.
- Actual: 외부 도구 재계산 미실행.
- Result: **PENDING**
- Evidence: 명세 known vector와 재계산 unit test PASS.

### 10. 반복 export ID

- Expected: 같은 pair를 다시 export하면 `exportId`/`exportedAt`은 달라도 `comparisonId`는 같다.
- Actual: native 앱에서 반복 저장 미실행.
- Result: **PENDING**
- Evidence: 독립 builder 반복 export unit test PASS.

### 11. deterministic payload

- Expected: 두 JSON의 `/exportId`, `/exportedAt`만 sentinel 처리하면 byte-for-byte 같다.
- Actual: 실제 저장 파일 두 개 비교 미실행.
- Result: **PENDING**
- Evidence: 정확히 두 JSON Pointer만 치환하는 unit test PASS; `comparisonId`는 검사에서 제외하지 않음.

### 12. Schema와 integrity validator

- Expected: 저장 JSON이 Draft 2020-12 Schema와 semantic/reference integrity validator를 모두 통과한다.
- Actual: 실제 저장 파일 대상 독립 실행 미실행.
- Result: **PENDING**
- Evidence: full synthetic export positive test와 13개 negative mutation, semantic corruption test PASS.

### 13. changes 내용 대조

- Expected: before/after, Anchor, full outline path와 character segment가 화면 비교 결과와 맞는다.
- Actual: 실제 pair 항목별 육안 대조 미실행.
- Result: **PENDING**
- Evidence: synthetic text/outline/table/image contract와 range/fingerprint 무결성 테스트 PASS.

### 14. 전체 목차 coverage

- Expected: 변경 없는 목차까지 포함하여 previous/latest 모든 목차 ID가 정확히 한 번씩 mapping된다.
- Actual: 실제 저장 JSON 수동 집계 미실행.
- Result: **PENDING**
- Evidence: synthetic 및 익명 301쪽 fixture coverage/integrity PASS.

### 15. 목차 관계와 ambiguity

- Expected: moved+modified, renamed, added, removed와 중복 제목의 `approximate` 사례가 실제 관계와 맞는다.
- Actual: 모든 관계를 포함한 수동 fixture 대조 미실행.
- Result: **PENDING**
- Evidence: moved+modified 및 duplicate-title confidence synthetic test foundation PASS; 실제 복합 사례 육안 검증 필요.

### 16. 병합 셀과 이미지 payload

- Expected: 병합 셀의 row/column/span과 image metadata가 정확하고 JSON에 binary가 없다.
- Actual: 대표 실제 병합 표/이미지 pair 대조 미실행.
- Result: **PENDING**
- Evidence: table/image 기존 회귀, Schema와 payload safety tests PASS.

### 17. 민감한 실행환경 정보 배제

- Expected: JSON에 local absolute path, 계정/credential, private profile 설정이 없다.
- Actual: 실제 저장 JSON 수동 검색 미실행.
- Result: **PENDING**
- Evidence: safety negative tests와 public/ignored path audit PASS.

### 18. 기존 기능 smoke

- Expected: rendering, Diff, Review Ink, navigation과 분리 창 workflow가 기존처럼 동작한다.
- Actual: 설치 앱 수동 smoke 미실행.
- Result: **PENDING**
- Evidence: 일반판 E2E 10 passed, local fidelity E2E 1 skipped.

### 19. 301쪽 문서

- Expected: 익명 301쪽 fixture pair에서 compare/export/open/validation을 완료하고 시간·메모리·파일 크기·UI 응답성을 확인한다.
- Actual: Node local integration에서는 same-file pair로 완료했으나 설치 앱 UI 수동 검증은 미실행.
- Result: **PENDING**
- Evidence: standalone run 기준 18,057,009 bytes, 301 pages, 3,646 paragraphs, 2,692 outlines; open 3,638.5 ms, snapshots 8,383.4 ms, diff 112.8 ms, export+integrity 1,048.8 ms, JSON 13,036,773 bytes, observed heap delta 58.1 MiB.

### 20. partial/failed analysis

- Expected: 안전한 fixture에서 `partial`/`failed` status와 warnings가 숨겨지지 않고 UI도 성공 결과처럼 표시하지 않는다.
- Actual: 현재 정상 비교 UI는 complete export만 제공하며 안전한 partial/failed UI fixture가 없어 미실행.
- Result: **PENDING**
- Evidence: Schema와 DTO는 status/warnings를 표현하지만 제품 UI 경로의 수동 검증이 필요함.

## Sign-off

- Tester:
- Windows version / hardware:
- Test date:
- HWPX pair identifier (원문이나 절대경로 금지):
- Overall result: **PENDING**
- Notes:
